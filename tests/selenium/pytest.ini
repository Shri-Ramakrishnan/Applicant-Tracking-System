[pytest]
pythonpath = .
testpaths = tests
addopts = -ra
markers =
    smoke: quick presentation-safe path
    auth: authentication coverage
    applicant: applicant journey coverage
    recruiter: recruiter journey coverage
    ui: UI validation coverage
